<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Akun - K-MAP</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;800&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

    <style>
        /* ANIMASI */
        @keyframes zoomEffect {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        @keyframes slideInUp {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        body {
            font-family: 'Poppins', sans-serif;
            height: 100vh;
            margin: 0;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            background-color: #000;
        }

        .bg-container {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: -2;
            background: url('https://images.unsplash.com/photo-1625246333195-58197bd47d26?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80') no-repeat center center;
            background-size: cover;
            animation: zoomEffect 30s infinite alternate ease-in-out;
        }

        .overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(135deg, rgba(0,0,0,0.7), rgba(10, 50, 80, 0.5));
            z-index: -1;
        }

        .register-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 25px;
            padding: 40px;
            width: 90%;
            max-width: 420px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.4);
            text-align: center;
            animation: slideInUp 0.8s ease-out;
        }

        .title {
            font-family: 'Montserrat', sans-serif;
            font-weight: 800;
            background: linear-gradient(#74b9ff, #fff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-transform: uppercase;
        }

        .form-control {
            background: rgba(0, 0, 0, 0.3) !important;
            border: 1px solid rgba(255, 255, 255, 0.15);
            color: white !important;
            border-radius: 12px;
        }

        .form-control:focus {
            background: rgba(0, 0, 0, 0.5) !important;
            border-color: #74b9ff;
            box-shadow: 0 0 10px rgba(116, 185, 255, 0.3);
        }

        label { color: rgba(255,255,255,0.6) !important; }

        .btn-register {
            background: linear-gradient(90deg, #0984e3, #74b9ff);
            border: none;
            padding: 12px;
            font-weight: 600;
            border-radius: 50px;
            transition: 0.3s;
            margin-top: 10px;
        }

        .btn-register:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(9, 132, 227, 0.4);
        }

        .login-link { color: rgba(255,255,255,0.5); text-decoration: none; font-size: 0.85rem; }
        .login-link:hover { color: #74b9ff; }

        .powered-by {
            position: fixed; bottom: 20px; font-size: 0.7rem; letter-spacing: 2px; opacity: 0.5;
        }
        .powered-by span { color: #74b9ff; font-weight: bold; }
    </style>
</head>
<body>

    <div class="bg-container"></div>
    <div class="overlay"></div>

    <div class="register-card">
        <div class="mb-3 text-info">
            <i class="bi bi-person-plus-fill" style="font-size: 2.5rem;"></i>
        </div>

        <h3 class="title">Join K-MAP</h3>
        <p class="text-white-50 mb-4 small">Daftar untuk memantau lahan pertanian</p>

        <% if (typeof message !== 'undefined' && message && message.length > 0) { %>
            <div class="alert alert-danger bg-danger bg-opacity-75 text-white border-0 py-2 small rounded-3 mb-3">
                <i class="bi bi-exclamation-octagon me-2"></i><%= message %>
            </div>
        <% } %>

        <form action="/register" method="POST">
            <div class="form-floating mb-3">
                <input type="text" name="fullname" class="form-control" id="fName" placeholder="Nama Lengkap" required>
                <label for="fName">Nama Lengkap</label>
            </div>
            <div class="form-floating mb-3">
                <input type="text" name="username" class="form-control" id="fUser" placeholder="Username" required>
                <label for="fUser">Username</label>
            </div>
            <div class="form-floating mb-3">
                <input type="password" name="password" class="form-control" id="fPass" placeholder="Password" required>
                <label for="fPass">Password</label>
            </div>

            <button type="submit" class="btn btn-primary w-100 btn-register">
                DAFTAR SEKARANG
            </button>
        </form>
        
        <div class="mt-4">
            <a href="/login" class="login-link">Sudah punya akun? <b class="text-white">Login</b></a>
        </div>
    </div>

    <div class="powered-by">
        @POWERED BY <span>KANZIE</span>
    </div>

</body>
</html>