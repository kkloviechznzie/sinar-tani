<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pasar Tani - Sinar Tani</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        body { background: #f0fdf4; font-family: 'Poppins', sans-serif; }
        .product-card { border: none; border-radius: 15px; transition: transform 0.2s; overflow: hidden; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .product-card:hover { transform: translateY(-5px); box-shadow: 0 10px 15px rgba(0,0,0,0.1); }
        .product-img { height: 200px; object-fit: cover; width: 100%; }
        .price { color: #198754; font-weight: bold; font-size: 1.1rem; }
        .btn-wa { background: #25D366; color: white; border: none; }
        .btn-wa:hover { background: #1da851; color: white; }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark bg-success sticky-top shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/"><i class="bi bi-arrow-left"></i> Kembali</a>
            <span class="navbar-text text-white fw-bold">🌽 PASAR TANI</span>
            <% if(user) { %>
                <button class="btn btn-light text-success fw-bold rounded-pill shadow-sm" data-bs-toggle="modal" data-bs-target="#jualModal">
                    <i class="bi bi-plus-lg"></i> Jual Panen
                </button>
            <% } else { %>
                <a href="/login" class="btn btn-outline-light rounded-pill">Login utk Jual</a>
            <% } %>
        </div>
    </nav>

    <div class="container mt-4 mb-5">
        <% if(products.length === 0) { %>
            <div class="text-center py-5 text-muted">
                <i class="bi bi-basket display-1"></i>
                <p class="mt-3">Belum ada hasil panen yang dijual.<br>Jadilah yang pertama!</p>
            </div>
        <% } %>

        <div class="row g-3">
            <% products.forEach(p => { %>
            <div class="col-6 col-md-3">
                <div class="card product-card h-100">
                    <img src="<%= p.photo %>" class="product-img">
                    <div class="card-body d-flex flex-column">
                        <h6 class="card-title text-truncate"><%= p.product_name %></h6>
                        <p class="price">Rp <%= p.price.toLocaleString('id-ID') %></p>
                        <p class="small text-muted mb-4"><%= p.description %></p>
                        
                        <div class="mt-auto d-grid gap-2">
                            <a href="https://wa.me/<%= p.seller_phone %>?text=Halo, saya mau beli <%= p.product_name %>..." target="_blank" class="btn btn-wa btn-sm">
                                <i class="bi bi-whatsapp"></i> Beli
                            </a>
                            <% if(user && (user.id === p.user_id || user.role === 'admin')) { %>
                                <a href="/pasar/delete/<%= p.id %>" class="btn btn-outline-danger btn-sm" onclick="return confirm('Hapus?')">Hapus</a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
    </div>

    <div class="modal fade" id="jualModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-success text-white">
                    <h5 class="modal-title">Jual Hasil Panen</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <form action="/pasar/add" method="POST" enctype="multipart/form-data">
                    <div class="modal-body">
                        <input type="text" name="product_name" class="form-control mb-2" placeholder="Nama Produk (Mis: Jagung Manis)" required>
                        <input type="number" name="price" class="form-control mb-2" placeholder="Harga (Rp)" required>
                        <textarea name="description" class="form-control mb-2" placeholder="Deskripsi singkat..." required></textarea>
                        <input type="number" name="seller_phone" class="form-control mb-2" placeholder="No. WA (08xxxx)" required>
                        <label class="small text-muted">Foto Produk</label>
                        <input type="file" name="product_photo" class="form-control" accept="image/*">
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-success w-100">Tayangkan Iklan</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>